
pre-requisites:


PostgreSQL devel package : postgresqlxx-devel(RHEL), postgresql-server-dev-x.x(Ubuntu)
PAM devel package : pam-devel(RHEL), libpam-devel(Ubuntu)
Readline devel or libedit devel package : readline-devel or libedit-devel(RHEL), libreadline-dev or libedit-dev(Ubuntu)
C compiler and build utility : "Development Tools" (RHEL), build-essential(Ubuntu)



download or clone from git 
git clone https://github.com/ossc-db/pg_bulkload.git

documentation:
http://ossc-db.github.io/pg_bulkload/pg_bulkload.html#install

make

make install


CREATE Extension pg_bulkload;


create a ctl file 

nano sample.ctl

TABLE = sample_test
TYPE = CSV
WRITER = DIRECT
INPUT = /home/chanukya/SetupFiles/sales_records.csv
SKIP = 1



usage:
/lib/postgresql/14/bin/pg_bulkload sample.ctl -o $'DELIMITER=,' -p 5445 -l pg_bulkload.log


connection options:

-d dbname
--dbname dbname
    Specifies the name of the database to be connected. If this is not specified, the database name is read from the environment variable PGDATABASE. If that is not set, the user name specified for the connection is used. 
-h host
--host host
    Specifies the host name of the machine on which the server is running. If the value begins with a slash, it is used as the directory for the Unix domain socket. 
-p port
--port port
    Specifies the TCP port or local Unix domain socket file extension on which the server is listening for connections.
-U username
--username username
    User name to connect as. 
-W
--password
    Force pg_bulkload to prompt for a password before connecting to a database.
    This option is never essential, since pg_bulkload will automatic
	
	
CREATE TABLE IF NOT EXISTS public.sample_test
(
    "Region" text COLLATE pg_catalog."default",
    "Country" text COLLATE pg_catalog."default",
    "Item Type" text COLLATE pg_catalog."default",
    "Sales Channel" text COLLATE pg_catalog."default",
    "Order Priority" character(5) COLLATE pg_catalog."default",
    "Order Date" text COLLATE pg_catalog."default",
    "Order ID" bigint,
    "Ship Date" text COLLATE pg_catalog."default",
    "Units Sold" numeric,
    "Unit Price" numeric,
    "Unit Cost" numeric,
    "Total Revenue" numeric,
    "Total Cost" numeric,
    "Total Profit" numeric
)